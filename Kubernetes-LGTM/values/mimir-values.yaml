# =============================================================================
# mimir-values.yaml — grafana/mimir-distributed chart 6.0.5
# Single-node lab deployment with S3 storage
# =============================================================================

fullnameOverride: mimir

minio:
  enabled: false

kafka:
  enabled: false

rollout_operator:
  enabled: false


mimir:
  structuredConfig:

    usage_stats:
      enabled: false

    # -------------------------------------------------------------------------
    # Object Storage
    # -------------------------------------------------------------------------

    common:
      storage:
        backend: s3
        s3:
          endpoint: s3.dualstack.${S3_REGION}.amazonaws.com
          region: ${S3_REGION}
          bucket_name: ${S3_BUCKET_MIMIR}

    blocks_storage:
      backend: s3
      s3:
        endpoint: s3.dualstack.${S3_REGION}.amazonaws.com
        region: ${S3_REGION}
        bucket_name: ${S3_BUCKET_MIMIR}

    alertmanager_storage:
      backend: s3
      s3:
        endpoint: s3.dualstack.${S3_REGION}.amazonaws.com
        region: ${S3_REGION}
        bucket_name: ${S3_BUCKET_MIMIR_ALERTMANAGER}

    ruler_storage:
      backend: s3
      s3:
        endpoint: s3.dualstack.${S3_REGION}.amazonaws.com
        region: ${S3_REGION}
        bucket_name: ${S3_BUCKET_MIMIR_RULER}

    # -------------------------------------------------------------------------
    # Ingestion
    # -------------------------------------------------------------------------

    ingester:
      ring:
        replication_factor: 1

    # -------------------------------------------------------------------------
    # Query configuration
    # -------------------------------------------------------------------------

    querier:
      max_concurrent: 8

    frontend:
      parallelize_shardable_queries: true

    # -------------------------------------------------------------------------
    # Limits
    # -------------------------------------------------------------------------

    limits:
      ingestion_rate: 10000
      ingestion_burst_size: 20000

      max_global_series_per_user: 100000

      max_fetched_chunks_per_query: 200000
      max_fetched_series_per_query: 100000


# =============================================================================
# Component Scaling
# =============================================================================

ingester:
  replicas: 1

  zoneAwareReplication:
    enabled: false

  persistentVolume:
    enabled: false

  resources:
    requests:
      cpu: 200m
      memory: 512Mi
    limits:
      cpu: 600m
      memory: 1Gi


distributor:
  replicas: 1

  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 400m
      memory: 256Mi


querier:
  replicas: 1

  resources:
    requests:
      cpu: 300m
      memory: 256Mi
    limits:
      cpu: 1
      memory: 1Gi


query_frontend:
  replicas: 1

  resources:
    requests:
      cpu: 200m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi


# query_scheduler intentionally disabled for single-node cluster
query_scheduler:
  replicas: 0


store_gateway:
  replicas: 1

  zoneAwareReplication:
    enabled: false

  persistentVolume:
    enabled: false

  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 1Gi


compactor:
  replicas: 1

  persistentVolume:
    enabled: false

  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi


alertmanager:
  replicas: 1

  persistentVolume:
    enabled: false

  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      cpu: 200m
      memory: 256Mi


ruler:
  replicas: 0


overrides_exporter:
  replicas: 1

  resources:
    requests:
      cpu: 20m
      memory: 64Mi
    limits:
      cpu: 100m
      memory: 128Mi


gateway:
  replicas: 1

  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi


# =============================================================================
# Cache configuration
# =============================================================================

chunks-cache:
  enabled: false

index-cache:
  enabled: false

metadata-cache:
  enabled: false

results-cache:
  enabled: true
  resources:
    requests:
      cpu: 20m
      memory: 64Mi
    limits:
      cpu: 100m
      memory: 256Mi


# =============================================================================
# Meta monitoring
# =============================================================================

metaMonitoring:
  serviceMonitor:
    enabled: false
  grafanaAgent:
    enabled: false


# =============================================================================
# Go runtime tuning
# =============================================================================

global:
  extraEnv:
    - name: GOMAXPROCS
      value: "2"
