fullnameOverride: tempo

deploymentMode: SingleBinary

# -----------------------------
# Single Binary Configuration
# -----------------------------
singleBinary:
  replicas: 1
  
  persistence:
    enabled: false  # Using S3 backend

  # Port naming for the Service
  service:
    type: ClusterIP
    port: 3200
    name: http-tempo
    targetPort: 3200
    protocol: TCP

# -----------------------------
# Tempo Application Config
# -----------------------------
tempo:
  auth_enabled: false

  server:
    http_listen_port: 3200
    grpc_listen_port: 9096
    log_level: info

  storage:
    trace:
      backend: s3
      s3:
        bucket: ${S3_BUCKET}/tempo
        region: ${S3_REGION}

  # Disabling metrics generator to save resources
  metricsGenerator:
    enabled: false

# -----------------------------
# Resource Management
# -----------------------------
resources:
  requests:
    cpu: 250m
    memory: 512Mi
  limits:
    cpu: 500m
    memory: 1Gi

# -----------------------------
# Health Probes
# -----------------------------
readinessProbe:
  httpGet:
    path: /ready
    port: http-tempo
  initialDelaySeconds: 30
  periodSeconds: 10

livenessProbe:
  httpGet:
    path: /ready
    port: http-tempo
  initialDelaySeconds: 60
  periodSeconds: 10
