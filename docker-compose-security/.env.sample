# Database Configuration
DD_DATABASE_NAME=defectdojo
DD_DATABASE_USER=defectdojo
DD_DATABASE_PASSWORD=your-secure-password-here

# Redis/Valkey Configuration (optional, defaults shown)
# DD_CELERY_BROKER_URL=redis://dojo-valkey:6379/0

# Security Keys - IMPORTANT: Generate secure random values for production
DD_SECRET_KEY=your-super-secret-key-min-50-chars-long
DD_CREDENTIAL_AES_256_KEY=your-32-character-aes-key-here

# Admin User Configuration
DD_ADMIN_USER=admin
DD_ADMIN_PASSWORD=your-secure-admin-password
DD_ADMIN_MAIL=admin@example.com

# Network Configuration
DD_ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# GitHub Runner (get token from GitHub: Settings > Developer settings > Personal access tokens > Fine-grained tokens)
# Token needs "Contents" and "Workflows" permissions for the repo
REPO_URL=https://github.com/your-org/your-repo
ACCESS_TOKEN=your-github-fine-grained-pat-token
RUNNER_NAME=security-scanner-runner
RUNNER_LABELS=self-hosted,security-scanner
